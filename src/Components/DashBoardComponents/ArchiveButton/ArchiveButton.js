import React from "react";
import StickyButton from "./StickyButton";

function ArchiveButton() {
  return (
    <div className="relative h-screen">
      {/* Your app content */}
      <StickyButton>Archive</StickyButton>
    </div>
  );
}

export default ArchiveButton;
